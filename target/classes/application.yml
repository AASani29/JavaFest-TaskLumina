spring:
  datasource:
    url: jdbc:postgresql://ep-long-flower-a157ef5v.ap-southeast-1.aws.neon.tech/tasks?user=tasks_owner&password=OFd7Ah2MtHQY&sslmode=require
  jpa:
    hibernate:
      ddl-auto: update

    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8080
